Follow the Hallmark design skill exactly as specified in `.claude/hallmark/SKILL.md`. Read that file now and execute the appropriate verb based on the user's input:

- No verb or unrecognized input → **Design flow** (default)
- `audit <target>` → load and follow `references/verbs/audit.md`
- `redesign <target>` → load and follow `references/verbs/redesign.md`
- `study <screenshot|URL>` → load and follow `references/study.md`

The skill file is the authoritative source. Read it before doing anything else.

Arguments: $ARGUMENTS
