const axios = require('axios');

async function run() {
  try {
    // Để gọi book, ta cần 1 slot open. Dùng 1 account patient hợp lệ
    console.log("Not implementing API trigger, I'll just write a script to emit event directly on SseService via Node REPL or write a dummy endpoint")
  } catch (e) {
    console.log(e);
  }
}
run();
